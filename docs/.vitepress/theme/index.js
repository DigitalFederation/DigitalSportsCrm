import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Plain VitePress default theme. (Earlier this theme hid code blocks and
// technical sections at runtime for a non-technical internal audience; that is
// removed here so the public developer documentation shows code, file paths,
// and commands as written.)
export default DefaultTheme
