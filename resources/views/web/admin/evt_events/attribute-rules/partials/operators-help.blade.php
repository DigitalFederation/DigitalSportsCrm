<div class="card sm:w-1/3 flex flex-col md:flex-row">
    <div class="p-4 border rounded-md shadow-sm bg-white">
        <h3 class="text-lg font-semibold mb-3">{{ __('How to Use Operators:') }}</h3>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Equal (==):') }}</span>
            {{ __('attribute_rules.operator_equal_example') }}
        </div>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Greater Than (>):') }}</span>
            {{ __('attribute_rules.operator_greater_than_example') }}
        </div>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Addition (+):') }}</span>
            {{ __('attribute_rules.operator_addition_example') }}
        </div>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Multiplication (*):') }}</span>
            {{ __('attribute_rules.operator_multiplication_example') }}
        </div>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Logical AND (&&):') }}</span>
            {{ __('attribute_rules.operator_and_example') }}
        </div>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Logical NOT (!):') }}</span>
            {{ __('attribute_rules.operator_not_example') }}
        </div>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Starts With (starts_with):') }}</span>
            {{ __('attribute_rules.operator_starts_with_example') }}
        </div>

        <div class="text-sm mb-3">
            <span class="font-medium text-blue-600">{{ __('Contains (contains):') }}</span>
            {{ __('attribute_rules.operator_contains_example') }}
        </div>
    </div>
</div>
