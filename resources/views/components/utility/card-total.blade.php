{{-- Disabled: Card total count section
<div class="hidden md:flex card flex-col items-center justify-center rounded-lg text-secondary my-5">
    <div class="text-5xl font-semibold leading-none tracking-tight">{{ $count }}</div>
    <div class="mt-1 text-lg font-medium text-center">{{ $title }}</div>
</div>
--}}
